# Helm Chart

```bash
$ git clone https://github.com/jclab-joseph/dnsever-rfc2136-bridge.git
$ cd dnsever-rfc2136-bridge/deploy/helm
$ vim values.yaml # authfile 부분 수정
$ helm install dnsever-rfc2136-bridge . -f values.yaml
```

